import 'package:flutter/material.dart';

class MailListPage extends StatelessWidget {
  const MailListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Mail list'));
  }
}
